extends layout 

block content 
  h1=title 

  if beatmaps.length > 0
    p You must first delete or change the modes on the following beatmaps to delete this mode: 
    ul 
      each beatmap in beatmaps 
        a(href=beatmap.url)
          li #{beatmap.fullname}

  else 
    p Are you sure you want to delete this mode?
    form(action="" method="post") 
      input#modeid(type='hidden' name='modeid' required='true' value=currentMode._id)
      button(type="submit") Yes